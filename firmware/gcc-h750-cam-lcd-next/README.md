# gcc-h750-cam-lcd-next

Experimental H750 firmware workspace for the post-MIWAI upgrades.

- Keep `../gcc-h750-cam-lcd-min/` as the known-good board bring-up / working AI baseline.
- Put new architecture, quantization, and rendering experiments here first.
- Only promote changes back into the stable firmware folder after they are verified on hardware.
